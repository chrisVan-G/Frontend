//Aplicación de objetos, matrices y funciones

function arrayBuilder(dateOne, dateTwo, dateThree) {
  let arr = [];
  arr.push(dateOne);
  arr.push(dateTwo);
  arr.push(dateThree);
  return arr;
}
let simpleArr = arrayBuilder(1, 2, 3);
console.log(simpleArr);
