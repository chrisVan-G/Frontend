const auto = {
  marca: 'autoXz',
  modelo: 2025,
  color: 'gris',
};

let { marca, color, modelo } = auto;
console.log(marca, modelo, color);

let numeros = [1, 2, 3];

let [uno, dos, tres] = numeros;
console.log(uno, dos, tres);
